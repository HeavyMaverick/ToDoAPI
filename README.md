# ToDo API

![Go Version](https://img.shields.io/badge/Go-1.25+-00ADD8?style=for-the-badge&logo=go)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-4169E1?style=for-the-badge&logo=postgresql&logoColor=white)
![Gin](https://img.shields.io/badge/Gin-00ADD8?style=for-the-badge&logo=go&logoColor=white)
![GORM](https://img.shields.io/badge/GORM-00ADD8?style=for-the-badge&logo=go&logoColor=white)

Простое и эффективное RESTful API для управления задачами (ToDo), построенное на Go с использованием PostgreSQL, GORM и Gin Framework.

## 📋 Возможности

-  **Полный CRUD** для задач
-  **PostgreSQL** для хранения данных
-  **Аутентификация** (планируется)
-  **Валидация данных**
-  **Логирование запросов**
-  **Гибкая архитектура** (слоистая структура)
-  **Поддержка двух хранилищ**: PostgreSQL и In-Memory
-  **Конфигурация через .env файлы**

## Архитектура

Проект следует чистой архитектуре с разделением на слои:
```
ToDoApi/
├── cmd/
│   └── api/
│       └── main.go                    # Точка входа
├── internal/
│   ├── config/                        # Конфигурация приложения
│   │   └── config.go
│   ├── database/                      # Работа с базой данных
│   │   └── database.go
│   ├── handler/                       # HTTP-обработчики (контроллеры)
│   │   └── handler.go
│   ├── model/                         # Модели данных
│   │   └── task.go
│   ├── repository/                    # Слой доступа к данным
│   │   ├── task_repository.go         # Интерфейсы и in-memory реализация
│   │   └── postgres_repository.go     # Postgres реализация
│   └── service/                       # Бизнес-логика (сервисный слой)
│       └── task_service.go            # Сервис для работы с задачами
├── .env
├── .gitignore
├── go.mod
├── go.sum
└── README.md
```

## ⚙️ Установка и настройка

### Предварительные требования

- **Go** 1.25 или выше
- **PostgreSQL** 18 
- **Git**

### 1. Клонирование репозитория

```bash
git clone https://github.com/heavymaverick/ToDoApi.git
```
### 2. Настройка конфигурации
Создайте файл .env в корне проекта:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=todo_dev

# Server Configuration
SERVER_PORT=8080
```
### 3. Запуск приложения
```bash
# Из корня проекта
go run cmd/api/main.go
```
