ToDo API
RESTful API для управления задачами (ToDo) на Go с использованием Gin, GORM и PostgreSQL.

📋 Оглавление
Особенности

Технологии

Требования

Установка и запуск

Конфигурация

API Документация

Структура проекта

Разработка

✨ Особенности
RESTful API с полным CRUD для задач

Поддержка PostgreSQL и In-Memory хранилища

Валидация данных и обработка ошибок

Автоматические миграции базы данных

Конфигурация через переменные окружения

Логирование SQL-запросов

Graceful shutdown

🛠 Технологии
Go 1.21+ - язык программирования

Gin - веб-фреймворк

GORM - ORM для работы с базой данных

PostgreSQL - реляционная база данных

Viper - управление конфигурацией

Docker (опционально) - контейнеризация

📦 Требования
Go 1.21 или выше

PostgreSQL 12+

Git

(Опционально) Docker и Docker Compose

🚀 Установка и запуск
1. Клонирование репозитория
bash
git clone https://github.com/heavymaverick/ToDoApi.git
cd ToDoApi
2. Установка зависимостей
bash
go mod download
3. Настройка базы данных PostgreSQL
Вариант A: Локальная установка
bash
# Создание базы данных
createdb todo_dev

# Или через psql
psql -U postgres -c "CREATE DATABASE todo_dev;"
Вариант B: Docker
bash
# Запуск PostgreSQL в Docker
docker run --name todo-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=todo_dev \
  -p 5432:5432 \
  -d postgres:15-alpine
4. Конфигурация
Создайте файл .env в корне проекта:

bash
cp .env.example .env
Отредактируйте .env файл:

env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=todo_dev
SERVER_PORT=8080
